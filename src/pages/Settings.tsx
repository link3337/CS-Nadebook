import React from "react";

export const Settings: React.FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <h2>Settings</h2>
      <p>Local-first persistence via Zustand. No cloud sync in V1.</p>
    </div>
  );
};

export default Settings;
